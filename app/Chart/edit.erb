<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @chart.user %></h1>
    <a href="<%= url_for :action => :show, :id => @chart.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @chart.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @chart.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="chart[user]" class="fieldLabel">User</label>
            <input type="text" id="chart[user]" name="chart[user]" value="<%= @chart.user %>" <%= placeholder( "User" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
